apps:
  - script   : run.bat
    instances: 4
    exec_mode: cluster
  - script : run.bat
    watch  : true
    env    :
      NODE_ENV: development
    env_production:
      NODE_ENV: production